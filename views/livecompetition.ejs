<%-include("partials/header")%>

<h1>Live</h1>
<div class="container" style="width:70%">
   <div class="row"> 
   <div class="competitor col-md-5">
       <div>
         <form action="/cordhome/<%=fest.festname%>/<%=registrations._id%>/<%=registrations.candidates[0]._id%>" method="POST">   
           <input type="text" name="score1">
           <button>Arrow</button>
           <%=registrations.candidates[start].id%>
         </form>  
       </div>
   </div>
   <div class="col-md-1" style="text-align: center;">
      VS
   </div>
   <div class="competitor col-md-5">
       <div>
         <form action="/cordhome/<%=fest.festname%>/<%=registrations._id%>/<%=registrations.candidates[1]._id%>" method="POST">
            <%=registrations.candidates[start+1].id%>
            <input type="text" name="score2">
           <button>Arrow</button>
         </form>
       </div>
   </div>
   </div>
</div>
   <% var i,n; %>
   <% if((registrations.candidates.length)%2 == 0){%>
         <% n = registrations.candidates.length;%>     
    <%}else{%>
         <% n = registrations.candidates.length-1;%>
    <%}%>
   <% for(i=start+2;i<n;i+=2) {%> 
           
            <div class>
            <%=registrations.candidates[i].id%>
            </div>
            <div>
             vs
            </div>
            <div>
           <%=registrations.candidates[i+1].id%>
           </div>
   <%}%>

<%-include("partials/footer")%>